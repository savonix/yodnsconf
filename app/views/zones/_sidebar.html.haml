.block.notice
  %h3 Status 
  .content
    %p= "Zones: #{Zone.total}"
    %p= "Expired: #{Zone.expired}"
.block
  %h3 Links
  %ul.navigation
    %li
      -if @zone.present?
        %a{:href => @zone.wwwhost, :target => "_blank"}= @zone.wwwhost 
      -else
        %a{:href => app_home_url, :target => "_blank"}= app_name
