.block
  %h2= "Check #{params[:id]}"
  .content
    .inner
      %table.table
        %thead
          %tr
            %th Check 
            %th Pass 
            %th Notes 
        %tbody
        - Zone.checks.each do |test|
          - check = @zone.send(test) 
          %tr
            %td= check.check_name
            %td= check.check_pass
            %td= to_table(check.check_results)

