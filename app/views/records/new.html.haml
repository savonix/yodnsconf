.block
  .secondary-navigation
    %ul.wat-cf
      %li.first= link_to "#{t("web-app-theme.list", :default => "List")}", records_path
      %li.active= link_to "#{t("web-app-theme.new", :default => "New")}", new_record_path
  .content
    %h2.title
      = t("web-app-theme.new", :default => "New")
      Record
    .inner
      = form_for :record, :url => records_path, :html => { :class => :form } do |f|
        = render :partial => "form", :locals => {:f => f}
- content_for :sidebar, render(:partial => 'sidebar')
